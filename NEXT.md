# NEXT

1. Remote (parked until auth): Origin namespace at https://cursor.com/codebase/get-started, OR `gh auth login` as bentruongclub then `gh repo create bentruongclub/fstack --private --source=/workspace/fstack --remote=origin --push`.
2. After remote: marketplace publish slice (plugin.json + README Install already local-proven).
