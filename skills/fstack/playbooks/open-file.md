# open-file

1. Read `NEXT.md`. The head item is the file to land.
2. Open that one file. Create it if missing.
3. Write only that file. Park anything else.
4. Prove it: file exists on disk. If it is `SKILL.md`, YAML frontmatter is valid. One-line check: an agent can follow it without a briefing.
5. Update `NEXT.md` to the following smallest slice.
